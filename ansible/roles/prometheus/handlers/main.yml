---
- name: Restart Prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: restarted
    daemon_reload: yes

- name: Reload Prometheus
  ansible.builtin.systemd:
    name: prometheus
    state: reloaded