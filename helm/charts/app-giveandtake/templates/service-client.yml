apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: client
  ports:
    - protocol: TCP
      port: 8091
      targetPort: 8091